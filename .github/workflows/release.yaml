name: Release

on:
  push:
    tags:
      - "*.*.*"

jobs:
  release:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v4
      - name: Bump the version
        run: npm version from-git
